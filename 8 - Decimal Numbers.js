var myDecimal = 8.7;

var mySecondDecimal = 0.0009;

// Multiplying:

var product = 2.0 * 2.5;
console.log(product)

// Dividing:

var quotient = 4.4 / 2.2;
console.log(quotient)

// Finding a remainder:

var remainder;
remainder = 11 % 3;
console.log(remainder)
